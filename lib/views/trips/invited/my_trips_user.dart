import 'package:flutter/material.dart';

class MyTripsViewUser extends StatelessWidget {
  const MyTripsViewUser({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
