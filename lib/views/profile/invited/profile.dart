import 'package:flutter/material.dart';

class ProfileViewInvited extends StatelessWidget {
  const ProfileViewInvited({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: const [
          Text('Profile'),
        ],
      ),
    );
  }
}
